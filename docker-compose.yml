name: projects.nickesselman.nl
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: projects.nickesselman.nl:prod
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOST: "0.0.0.0"
    ports:
      - "127.0.0.1:3018:3000"
    working_dir: /app
